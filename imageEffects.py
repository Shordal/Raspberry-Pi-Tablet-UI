#Tablet UI

class cameraImageEffects(object):
    
    def cameraNegative(camera):
            camera.image_effect = 'negative'
    def cameraSolarize(camera):
            camera.image_effect = 'solarize'
    def cameraSketch(camera):
            camera.image_effect = 'sketch'
    def cameraDenoise(camera):
            camera.image_effect = 'denoise'
    def cameraEmboss(camera):
            camera.image_effect = 'emboss'
    def cameraOilpaint(camera):
            camera.image_effect = 'oilpaint'
    def cameraHatch(camera):
            camera.image_effect = 'hatch'
    def cameraGpen(camera):
            camera.image_effect = 'gpen'
    def cameraPastel(camera):
            camera.image_effect = 'pastel'
    def cameraWatercolor(camera):
            camera.image_effect = 'watercolor'
    def cameraFilm(camera):
            camera.image_effect = 'film'
    def cameraBlur(camera):
            camera.image_effect = 'blur'
    def cameraSaturation(camera):
            camera.image_effect = 'saturation'
    def cameraColorswap(camera):
            camera.image_effect = 'colorswap'
    def cameraWashedout(camera):
            camera.image_effect = 'washedout'
    def cameraPosterise(camera):
            camera.image_effect = 'posterise'
    def cameraColorpoint(camera):
            camera.image_effect = 'colorpoint'
    def cameraColorbalance(camera):
            camera.image_effect = 'colorbalance'
    def cameraCartoon(camera):
            camera.image_effect = 'cartoon'
    def cameraDeinterlace1(camera):
            camera.image_effect = 'deinterlace1'
    def cameraDeinterlace2(camera):
            camera.image_effect = 'deinterlace2'
    def cameraNone(camera):
            camera.image_effect = 'none'
